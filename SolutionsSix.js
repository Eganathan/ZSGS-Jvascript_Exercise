//sort Array lowest to largest
const nums = [1,4,3,2,5,6];

sortAssending(nums);
sortDessending(nums);

function sortAssending(arr)
{
    console.log(arr.sort());
}

//sort array in reverse order
function sortDessending(arr)
{
    console.log(arr.reverse());
}
